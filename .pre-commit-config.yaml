default_language_version:
  node: system

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: check-added-large-files
        args: ["--maxkb=15000"]
      - id: check-toml
        types: [toml]
      - id: check-yaml
        # mkdocs.yml uses Material's !!python/ tags (pymdownx.slugs.slugify,
        # superfences fence_code_format). --unsafe checks syntax without trying
        # to resolve custom constructors.
        args: ["--unsafe"]
      - id: end-of-file-fixer
      - id: trailing-whitespace

  - repo: https://github.com/pre-commit/mirrors-prettier
    rev: v4.0.0-alpha.8
    hooks:
      - id: prettier

  - repo: https://github.com/asottile/blacken-docs
    rev: 1.19.1
    hooks:
      - id: blacken-docs

  - repo: https://github.com/charliermarsh/ruff-pre-commit
    rev: "v0.12.7"
    hooks:
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix, --preview, --unsafe-fixes]

      - id: ruff-format
